const styles = () => ({
    formControl: {
        margin: theme.spacing(1),
        width: 200,
    },
    selectEmpty: {
        marginTop: theme.spacing(2),
    },
    listViewContainer: {
        padding: '10px'
    }
});
export default styles;